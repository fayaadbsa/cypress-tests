{
  "name": "cypress-tests",
  "version": "1.0.0",
  "description": "Cypress Unit and E2E UI Tests",
  "main": "index.js",
  "scripts": {
    "test": "cypress run",
    "test:api": "cypress run --spec \"cypress/tests/api/**/*\"",
    "test:ui": "cypress run --spec \"cypress/tests/ui/**/*\"",
    "cypress:open": "cypress open"
  },
  "devDependencies": {
    "cypress": "^13.12.0"
  }
}
